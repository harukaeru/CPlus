#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int a, b;
    // cout << "直角をはさむ2辺の長さを入力してください: " << flush;
    // cin >> a >> b;

    cout << "斜辺の長さは" << sqrt(a * a + b * b) << "です" << endl;

    cout << "mod(100, 10)" << fmod(100, 10) << endl;
    cout << "pow(2, 10)" << pow(2, 10) << endl;
}
